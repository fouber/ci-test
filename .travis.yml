language: node_js
node_js:
  - "0.10"
env:
  - PATH=node_modules/.bin/:$PATH
install:
  - "npm install fis"
  - "fis -v"
script:
  - "fis release -Doumpd output"
after_success:
  - "sh build.sh"
cache:
  directories:
    - node_modules
branches:
  only:
    - master